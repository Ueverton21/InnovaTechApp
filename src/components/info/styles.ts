import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  line_info: {
    flexDirection: "row",
    marginTop: 10,
    alignItems: "center",
  },
  text_default: {
    fontSize: 14,
    color: "#999",
    marginLeft: 4,
    fontWeight: "bold",
  },
});
